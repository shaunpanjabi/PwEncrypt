<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/action_spinner"
        android:icon="@android:drawable/ic_menu_search"
        android:title="Search"
        app:showAsAction="always|collapseActionView"
        app:actionViewClass="android.support.v7.widget.SearchView" />

    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        app:showAsAction="never"/>

    <item android:id="@+id/view_all_pwds_settings"
        android:title="@string/view_all_pwds_settings"
        app:showAsAction="never"/>

    <item android:id="@+id/import_export_settings"
        android:title="@string/import_export_settings"
        app:showAsAction="never"/>
</menu>